{
  "name": "village-water-system",
  "version": "1.0.0",
  "description": "Village Water System - Full Stack Application",
  "main": "backend/manage.py",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python manage.py runserver 0.0.0.0:8000",
    "frontend": "cd frontend && npm start",
    "migrate": "cd backend && python manage.py migrate",
    "createsuperuser": "cd backend && python manage.py createsuperuser"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
